{{- $fullName := include "product-catalog.fullname" . -}}
🎉 {{ .Chart.Name }} has been deployed!

📋 Release: {{ .Release.Name }}
📦 Version: {{ .Chart.AppVersion }}
🏷️  Namespace: {{ .Release.Namespace }}

🔌 Service endpoint:
   {{ $fullName }}:{{ .Values.service.port }}

To verify the deployment:
  kubectl get pods -l "app.kubernetes.io/name={{ include "product-catalog.name" . }},app.kubernetes.io/instance={{ .Release.Name }}" -n {{ .Release.Namespace }}

To check logs:
  kubectl logs -l "app.kubernetes.io/name={{ include "product-catalog.name" . }}" -n {{ .Release.Namespace }} -f
